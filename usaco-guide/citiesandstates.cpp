#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    fstream file("citystate.in");

    int N; stoi(getline(file, N));
}