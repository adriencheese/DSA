#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int N = 1; cin >> N;
    while (N != 0) {
        for (int i = 0; i < N; i++) {
            string line; getline(cin, line);
            

            
        }

        cin >> N;
    }




}
